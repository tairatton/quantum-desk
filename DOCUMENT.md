# Quantum Desk — เอกสารระบบฉบับเต็ม

ปรับปรุงล่าสุด 6 สิงหาคม 2026 · หลัง refactor แยกสองต้นไม้

---

## 1. ระบบนี้คืออะไร

บอทเทรดอัตโนมัติสำหรับ **บัญชีทดสอบ prop firm** สองเจ้าพร้อมกัน โดยใช้กลยุทธ์เดียวกัน

| | forex | future |
|---|---|---|
| Prop firm | FTMO 2-Step | TopStep Combine |
| ช่องทาง | MetaTrader 5 | ProjectX Gateway (REST) |
| สินค้า | XAUUSD spot | MGCZ26 (Micro Gold, 10 oz) |
| หน่วยไซซ์ | lots | contracts (จำนวนเต็ม) |
| หน่วยความเสี่ยง | % ของทุนตั้งต้น | ดอลลาร์ |
| ขนาดบัญชี | $50,000 | $50,000 |
| สถานะ | **รันจริงอยู่** | **ยังไม่ commission** |

สินค้าอ้างอิงเป็นทองคำทั้งคู่ ต่างกันที่รูปแบบสัญญา — spot 1 lot = $100 ต่อจุด, MGC 1 สัญญา = $10 ต่อจุด (หนึ่งในสิบ)

### กลยุทธ์

HTF Quantum Adaptive — ตรวจจับโครงสร้างราคาบน M15 และ M30 แล้ววางแผนเข้าไม้พร้อมเป้าสามระดับ โค้ดกลยุทธ์อยู่ที่ `strategy/quantum.py` และบอทเรียกใช้ตัวเดียวกับที่ backtest ใช้ ป้องกันไม่ให้ระบบที่รันจริงเพี้ยนไปจากระบบที่วัดผลไว้

### เทคนิคออก (production ทั้งสอง venue)

`be_after_tp1_33_33_34` — แบ่งไม้เป็นสามขา 33/33/34

1. TP1 โดน → ขาที่เหลือย้าย SL ไป **breakeven ที่ครอบคลุมต้นทุน** (ราคาที่ fill จริง + commission + slippage + swap สะสม ไม่ใช่ราคาเข้า)
2. TP2 โดน → ขาสุดท้ายย้าย SL ขึ้นไป **ล็อกที่ระดับ TP1**
3. ขาสุดท้ายวิ่งไป TP3

ถ้าไซซ์เล็กเกินจะแตกสามขา (forex: ทุน < $30,000 · future: < 3 สัญญา) จะใช้ `fixed_tp3` ขาเดียววิ่งยาว — ไม่ใช่การประนีประนอมสองขาที่ไม่เคยถูกวัดผล

### การจัดการความเสี่ยง

Dynamic risk ladder — ไต่ลงตาม drawdown จาก high-water mark ของยอดปิดจริง

| ขั้น | เงื่อนไข (forex) | forex | เงื่อนไข (future) | future |
|---|---|---|---|---|
| 1 | DD < 0.50% | 1.00% | DD < $250 | $500 |
| 2 | DD < 1.00% | 0.75% | DD < $500 | $375 |
| 3 | DD < 1.50% | 0.50% | DD < $750 | $250 |
| พื้น | DD ≥ 1.50% | 0.40% | DD ≥ $750 | $200 |

กฎเดียวกันเป๊ะ ต่างแค่หน่วย เพราะ FTMO คิดเป็น % ของทุน ส่วน TopStep เป็นดอลลาร์คงที่ที่ไม่ขยับตามยอดเงิน

---

## 2. ทำอะไรได้บ้าง

### ฝั่ง forex

```bash
cd forex
python -m bot.run --status      # สถานะบัญชี guard และสถิติ R จริง
python -m bot.run --once        # เดินหนึ่งรอบ dry run
python -m bot.run --live        # ส่งออเดอร์จริง
python -m bot.run --flatten --live   # ฉุกเฉิน ยกเลิกและปิดทุกอย่าง
python -m bot.main              # เมนูภาษาไทย
python tools/ftmo_portfolio_sim.py   # ซิมอัตราสอบผ่านและ DD
python main.py backtest         # วัดผลทุก symbol × timeframe
```

### ฝั่ง future

```bash
cd future
python -m bot.terminal                    # เทอร์มินัลเมนู
python -m bot.terminal --status --offline # สถานะหน้าเดียว ไม่ต้องต่อเน็ต
python -m bot.live --check                # ทดสอบเชื่อมต่อ อ่านอย่างเดียว
python tools/topstep_sim.py --ladder      # ซิมอัตราสอบผ่านตามกฎ TopStep
```

### สิ่งที่บอทดูแลให้อัตโนมัติ

- ปิด/เปิดตามเวลาตลาดและวันหยุด รวมถึงช่วงพักบำรุงรักษาของ CME
- หยุดเข้าไม้รอบข่าว USD ระดับสูง (fail closed — ถ้าดึงปฏิทินไม่ได้ ห้ามเข้าไม้ ไม่ใช่ปล่อยผ่าน)
- นับวันเทรด นับ request โควตา และกันไม่ให้รันซ้อนสองโปรเซส
- state ทนการรีสตาร์ต — high-water mark ไม่ถูกรีเซ็ตกลับเป็นบัญชีใหม่
- kill switch เป็นไฟล์ — สร้างไฟล์ `STOP` แล้วหยุดเข้าไม้ใหม่ทันที ไม้เดิมยังถูกดูแลต่อ

---

## 3. ผลลัพธ์ที่วัดได้

### 3.1 ผลกลยุทธ์ดิบ (XAUUSD, holdout ที่ไม่เคยใช้เลือกเทคนิค)

| TF | ไม้ | Win rate | Expectancy | PF | Max DD (R) |
|---|---|---|---|---|---|
| M15 | 248 | 40.3% | +0.176R | 1.49 | 9.07R |
| M30 | 227 | 57.7% | +0.357R | 2.07 | 6.09R |

รวมทั้ง book: **+0.236R ต่อไม้ · 3.1 ไม้ต่อวัน · +0.721R ต่อวัน**

### 3.2 FTMO — อัตราสอบผ่าน

20,000 paths, 400 วัน, corr 0.3, risk 0.40%

| Regime | ผ่าน Step 1 | breach | ผ่านทั้ง 2 step | วันที่ใช้ (med/p90) | DD med | DD p99 |
|---|---|---|---|---|---|---|
| holdout (ปัจจุบัน) | 100.0% | **0.0%** | 100.0% | **53 / 75** | 2.64% | 5.11% |
| validation | 100.0% | 0.0% | 100.0% | 44 / 60 | 2.23% | 4.23% |
| train (แบน 2023) | 100.0% | 0.0% | 100.0% | 91 / 144 | 4.13% | 8.44% |

**สรุป: ไม่มี path ไหนสอบตก ผ่านทั้งสองด่านใน ~2.5 เดือน**

### 3.3 TopStep — อัตราสอบผ่าน

20,000 paths, ladder เปิด, internal stop $400

| Regime | ผ่าน | ตก | วันที่ใช้ (med/p90) | DD med | DD p95 |
|---|---|---|---|---|---|
| holdout | **99.8%** | 0.2% | **9 / 19** | $400 | $1,149 |
| validation | 100.0% | 0.1% | 7 / 15 | $400 | $973 |
| train | 97.7% | 2.4% | 13 / 33 | $680 | $1,778 |

ถ้าใช้ flat $200 แทน ladder: ผ่าน 99.9%, 20/33 วัน, DD p95 $1,812

**ทำไม TopStep เร็วกว่าเกือบ 6 เท่า** — ไม่ใช่เพราะระบบดีกว่า แต่เป้าเล็กกว่า: $3,000 = 6% ครั้งเดียว เทียบกับ FTMO 10% แล้วต่อ 5% อีกรอบ ที่ +$361/วัน เท่ากัน $3,000 ÷ 361 ≈ 8.3 วัน ตรงกับ median 9

### 3.4 DD ต่อวันและ Max DD

| | forex (FTMO) | future (TopStep) |
|---|---|---|
| DD ต่อวัน — บอทหยุดเอง | 1.50% ($750) | $400 (0.80%) |
| DD ต่อวัน — เพดาน firm | 5.00% ($2,500) | $1,000 (2.00%) |
| Max DD median | 2.64% | $400 (0.80%) |
| Max DD p95–p99 | 5.11% (p99) | $1,149 (p95) |
| Max DD regime แบน | 8.44% (p99) | $1,778 (p95) |
| เพดานที่ทำให้ตก | 10.00% ($5,000) | $2,000 (4.00%) |
| **ใช้ไปกี่ % ของระยะที่ตายได้** | ~51% | ~57% |

---

## 4. ความเสี่ยง

### 4.1 ความเสี่ยงที่วัดเป็นตัวเลขได้ — edge หด

ตัวเลขสอบผ่านทั้งหมดตั้งอยู่บนสมมติฐานว่า edge ยังอยู่ที่ +0.236R/ไม้ ถ้าหดลง

| edge เหลือ | FTMO ตก | TopStep ตก |
|---|---|---|
| +0.236R (ที่วัดได้) | 0.0% | 0.1% |
| +0.150R | 0.0% | 1.5% |
| +0.100R (หายไป 58%) | 0.2% | 6.6% |
| +0.050R | 4.2% | **23.6%** |
| +0.020R | 23.3% | 44.0% |
| 0.000R | 48.2% | 59.0% |
| −0.050R | 95.7% | 88.5% |

**TopStep เปราะกว่ามากเมื่อ edge หด** เพราะ max loss เป็น trailing — กำไรที่เพิ่งทำได้ดันพื้นขึ้นตามหลัง คืนกลับไปก็ตายทั้งที่บัญชียังกำไรอยู่ ส่วน FTMO พื้นนิ่งที่เดิม กำไรจึงเป็นกันชนถาวร

### 4.2 ความเสี่ยงเชิงโครงสร้าง

| ความเสี่ยง | ผลกระทบ | สถานะการป้องกัน |
|---|---|---|
| **edge ของ future ยังไม่ได้วัดบน MGC จริง** | ตัวเลข 99.8% อาจไม่จริงเลย | ยังไม่แก้ — `--live` ถูกล็อกไว้ |
| **endpoint ProjectX ยังไม่เคยยิงจริง** | ออเดอร์อาจไม่ออกหรือออกผิด | `--live` ปฏิเสธจนตั้ง `COMMISSIONED = True` |
| **ตัวเลขกฎ TopStep มาจากแหล่งรอง** | ถ้ากฎเปลี่ยน บอทจะคิดว่าปลอดภัยทั้งที่ตายแล้ว | มีคอมเมนต์เตือนทุกจุด ต้องเทียบ rulebook |
| **MLL เช็ค real-time รวมกำไรลอยตัว** | ซิมเห็นแค่ยอดรายวัน จึงมองโลกสวยตรงกฎที่ฆ่าบัญชี | ยังไม่จำลอง |
| **consistency rule** | median วันดีสุด $1,369 ห่างเพดาน $1,500 แค่ 9% | เกินแล้วเป้าขยับขึ้น ไม่ตก แต่ยืดเวลา |
| **engine/strategy ถูกก๊อปสองชุด** | แก้บั๊กที่หนึ่งไม่ใช่แก้ที่สอง | ตั้งใจแลกเพื่อแยกขาด — ต้องแก้สองที่ทุกครั้ง |
| **roll สัญญาเป็น manual** | MGCZ26 หมดอายุแล้วบอทไม่รู้ | broker ปฏิเสธถ้าเจอหลาย expiry ต้องเปลี่ยนมือ |
| **spread FTMO แพงกว่า backtest 2 เท่า** | $0.40–0.42 vs $0.20 ใน Exness | คิดเข้าไปแล้ว: −0.012 ถึง −0.018R/ไม้ |

### 4.3 ความเสี่ยงที่ซิมไม่ครอบคลุมเลย

1. **การปัดเป็นสัญญาเต็มใบ** — ซิมคิดความเสี่ยงเป็น $200 ต่อเนื่อง ของจริงกระโดดเป็นขั้น และมักต่ำกว่าที่ขอ
2. **gap ข้ามคืน/สุดสัปดาห์** — SL อาจถูกข้าม ขาดทุนเกินที่คำนวณ
3. **feed ค้าง ไฟดับ เน็ตหลุด** — มี stale-feed guard แต่ไม่ได้อยู่ในซิม
4. **slippage หางยาว** — ใช้ค่าประมาณ ไม่ใช่ค่าที่วัดจริงจาก FTMO demo
5. **ทั้งสองบัญชีเทรดทองเหมือนกัน** — ถ้ารันพร้อมกันคือความเสี่ยงทางเดียวกันสองเท่า ไม่ใช่การกระจาย

---

## 5. โครงสร้างโปรแกรม

```
quantum-desk/
├── forex/                      FTMO · MT5 · XAUUSD
│   ├── bot/       settings · broker(MT5) · guardrails(FTMO) · run · trader · main · live
│   ├── engine/    instrument · sizing · state · journal · news · market_hours · dynamic_risk
│   ├── strategy/  quantum · technique_lab · backtest_reporting · webapp · mt5_source
│   ├── tools/     ftmo_portfolio_sim · build_ftmo_report · plot · forward_check · launch
│   ├── test/      unit(278) · docs · data · outputs · pine
│   └── main.py
└── future/                     TopStep · ProjectX · MGCZ26
    ├── bot/       settings · broker(ProjectX) · guardrails(TopStep) · trader · terminal · live
    ├── engine/    (สำเนา + decide_dollars)
    ├── strategy/  (สำเนา)
    ├── tools/     topstep_sim
    └── test/      unit(48) · docs · data · outputs
```

**กฎเหล็ก: ไม่มี import ข้ามต้นไม้** ตรวจแล้วเป็นศูนย์ แม้แต่ตัวแปรสภาพแวดล้อมยังแยก — forex อ่าน `BOT_*` future อ่าน `FUT_*` การแก้ฝั่งฟิวเจอร์สจึงไปแตะบัญชี FTMO ที่รันเงินจริงอยู่ไม่ได้

**ราคาที่จ่าย:** `engine/` และ `strategy/` มีสองชุด แก้บั๊กต้องแก้สองที่

---

## 6. บั๊กที่พบและแก้ในรอบนี้

### จาก refactor (path)

| # | บั๊ก | ผลถ้าไม่แก้ |
|---|---|---|
| 1 | `ftmo_portfolio_sim` โหลด `bot/code/settings.py` ผ่าน importlib | ซิมพังทั้งตัว — grep หา `bot.code` ไม่เจอเพราะเป็น path string |
| 2 | `main.bat` cd ผิดระดับ + `.venv` path ผิด | ดับเบิลคลิกแล้วบอทไม่ขึ้น |
| 3 | `test_quantum_entry` hardcode `ROOT/"data"/"market"` | เทสต์แดงหลังย้ายโฟลเดอร์ |
| 4 | `webapp.py` ชี้ `templates/` เดิม | หน้าเว็บ 500 |
| 5 | `future/test/` ไม่มีข้อมูล backtest | `topstep_sim` รันไม่ได้ |
| 6 | `__pycache__` 71 ไฟล์ + `.pytest_cache` ถูก commit ทั้งที่อยู่ใน .gitignore | repo สกปรก diff รก |

### บั๊กตรรกะในโค้ดฟิวเจอร์สใหม่ (ร้ายแรงกว่า)

| # | บั๊ก | ผลถ้าไม่แก้ |
|---|---|---|
| 7 | `split_contracts` ตัดเศษทิ้งแล้วโยนให้ขาสุดท้าย — 6 สัญญาได้ **1/1/4** แทน 2/2/2 | รันคนละระบบกับที่วัดผล สองในสามของไม้วิ่งไป TP3 แทนที่จะเป็นหนึ่งในสาม |
| 8 | `session_open` เช็คแค่วันเสาร์ | **เช้าวันอาทิตย์ถูกนับว่าตลาดเปิด** ทั้งที่ยังไม่เปิดอีก 8 ชั่วโมง |
| 9 | `session_open` บล็อกทุกอย่างหลัง 15:10 | **ทิ้งเซสชันกลางคืนทั้งหมด** ที่ M15/M30 เทรดอยู่ |
| 10 | `live.py` ส่งเวลาท้องถิ่นแบบ naive เข้า guardrails | เครื่องที่กรุงเทพคลาด **12 ชั่วโมง** ประตูเวลาตอบผิดครึ่งวัน |
| 11 | ไม่มี TP2 step | ขาสุดท้ายค้างที่ BE ไม่ล็อก TP1 — คนละเทคนิคกับที่ซิมคิด |
| 12 | ไม่มี dynamic risk ladder | เสี่ยงคงที่ ไม่หดตอน drawdown |

ทุกข้อมีเทสต์กันถอยหลังแล้ว

---

## 7. เทอร์มินัล

`python -m bot.terminal` — ออกแบบให้ตอบสามคำถามในหน้าจอเดียว: เหลือระยะเท่าไรก่อนตก, ตลาดเปิดให้เข้าไม้ไหม, ไม้ถัดไปสั่งกี่สัญญา

```
==============================================================================
  FUTURES · TopStep · MGC · M15, M30
  Thu 2026-08-06 01:49 CDT  exchange clock
==============================================================================
[CONNECTION] not attempted
[SESSION] ok
[HEALTH] ok
[COMMISSIONED] no — --live is refused until the checklist is done

  Balance               $51,500.00                    live
  Started at            $50,000.00                    high water $51,500.00 EOD
  Max loss floor        $49,500.00                    trailing end-of-day
  Room to the floor     $2,000.00                     ............................
  Today's loss          $0.00 of $1,000               ............................
  Internal stop         $50,800.00                    bot stands down at -$400
  Target                $+1,500.00 of $3,000          ##############..............

  Risk per trade        $500                          drawdown ladder
    stop 5 pts          5 contracts                   $250 real risk · 1/2/2
    stop 10 pts         5 contracts                   $500 real risk · 1/2/2
    stop 20 pts         2 contracts                   $400 real risk · 1 leg to TP3
    stop 40 pts         1 contract                    $400 real risk · 1 leg to TP3
==============================================================================
```

หลักการออกแบบ

- **วาดได้โดยไม่ต้องต่อเน็ต** — หน้าจอที่ว่างเปล่าตอนเน็ตหลุดคือหน้าจอที่ใช้ไม่ได้ตอนที่ต้องใช้ที่สุด `--offline` อ่านจาก state file ล้วน
- **แถบแทนตัวเลข** สำหรับเพดาน — ตัวเลขบอกว่า $1,149 จาก $2,000 แถบบอกทันทีว่าเกินครึ่งทางไปหาพื้นแล้ว สีเปลี่ยนที่ 50% และ 80%
- **แสดงความเสี่ยงจริงหลังปัด ไม่ใช่ที่ขอ** — panel ออเดอร์คำนวณสี่ระยะ stop และบอกตรง ๆ ว่าอันไหน "no trade"
- **ปิดสีเมื่อไม่ใช่ terminal** — pipe ลง log ได้สะอาด
- คำสั่งที่ส่งเงินออกต้องพิมพ์คำยืนยันเป็นตัวพิมพ์ใหญ่ ไม่ใช่กด y

---

## 8. สถานะการทดสอบ

| ต้นไม้ | เทสต์ | ผล |
|---|---|---|
| forex | 278 + 19 subtests | ผ่านทั้งหมด |
| future | 48 | ผ่านทั้งหมด |

ตรวจเพิ่มหลัง refactor: import ทุกโมดูลสองต้นไม้ผ่าน · path ที่ resolve จริงทุกจุดมีไฟล์อยู่ · entry point ทุกตัวรันได้ · `bot.run --status` อ่าน state เดิมได้ครบ · ไม่มี import ข้ามต้นไม้

---

## 9. สิ่งที่ยังไม่เสร็จ

1. **`future/bot/run.py` ยังไม่มี** — ฝั่งฟิวเจอร์สมีตั้งแต่ signal → sizing → ส่งออเดอร์ และมีเทอร์มินัล แต่ยังไม่มี loop ที่ดึงบาร์ MGC จัดการ state/journal และ manage ขาหลัง TP1/TP2
2. **`eod_balance_high_water` ยังไม่มีใครเขียน** — guardrails อ่านแล้ว แต่ต้องมี run loop มาอัปเดตทุกสิ้นวัน
3. **backtest บน MGC จริง** — ยังใช้ผลของ XAUUSD spot อยู่
4. **ยืนยันกฎ TopStep กับ rulebook ทางการ**
5. **ทดสอบ ProjectX endpoint กับ demo key**
6. **โหมด `--ladder` ในซิมฝั่ง FTMO** — ตอนนี้ให้ได้แค่กรอบบน-ล่าง (0.40% / 1.00%) ไม่ใช่ค่าจริงค่าเดียว
7. ยังไม่ commit — ทุกอย่างอยู่ใน working tree

---

## 10. เช็คลิสต์ก่อนเปิดบัญชีฟิวเจอร์สจริง

- [ ] `python -m bot.live --check` ผ่านกับ demo key และชื่อบัญชี/สัญญาถูกต้อง
- [ ] เทียบ daily loss / max loss / profit target / เวลา flat-by ของโลหะ กับ rulebook ทางการ
- [ ] ดึงข้อมูล MGC ลง `future/test/data/market/` แล้ววัดผลใหม่ด้วย technique lab
- [ ] ตรวจว่า stop ตาม ATR ที่ใช้จริงกว้างพอให้ได้ 3 สัญญา ไม่งั้นการแตกสามขาจะไม่เคยทำงาน
- [ ] เขียน `run.py` และทดสอบ dry run เต็มวันเทรด
- [ ] ตั้ง `COMMISSIONED = True` เป็นขั้นตอนสุดท้าย
